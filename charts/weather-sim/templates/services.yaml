apiVersion: v1
kind: Service
metadata:
  name: weather-sim-api
spec:
  selector: { app: weather-sim-api }
  ports: [{ port: 8080, targetPort: 8080 }]
---
apiVersion: v1
kind: Service
metadata:
  name: weather-sim-web
spec:
  selector: { app: weather-sim-web }
  ports: [{ port: 80, targetPort: 80 }]
