jobs:
  - job: build_test
    pool: { vmImage: ubuntu-latest }
    steps:
      - task: NodeTool@0
        inputs: { versionSpec: '22.x' }
      - script: npm install
      - script: npm run build
      - script: npm run test
