apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "kibana.fullname" . }}
  labels:
    {{- include "kibana.labels" . | nindent 4 }}
data:
  kibana.yml: |-
{{- tpl (.Files.Get "kibana.yml") . | nindent 4 }}
